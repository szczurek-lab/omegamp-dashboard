__pycache__/
*.pyc
.DS_Store
*.egg-info/
.venv/
docs/index_unprotected.html
.staticrypt.json
.idea/
data.zip

# Raw experimental data and rendered figures -- not tracked until publication.
# See README for how to request access.
# Exception: the reference table is the single source of truth for the
# peptide id->name->sequence mapping and IS tracked (a gitignored copy is how
# the prototype label swap went unnoticed).
data/*
!data/omegamp_reference_table.csv
figures/

# Notebook checkpoints (notebooks themselves are tracked) + local path overrides
notebooks/.ipynb_checkpoints/
paths.local.sh

# Local archive of superseded / reproduction-process files (not committed)
archive/

# Stray data output from running wetlab scripts inside scripts/ (data stays out of the repo)
scripts/data/
